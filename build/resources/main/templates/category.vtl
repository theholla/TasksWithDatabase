<h2 class="well">category: $category.getName()</h2>

<ol>
	#foreach ($task in $category.getTasks())
			<li><h3>$task.getDescription()</h3></li>
	#end
</ol>
<hr/>
<a class="btn btn-primary" href="/categories/$category.getId()/tasks/new" role="button">Add another task to $category.getName()</a>
